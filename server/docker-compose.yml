services:
  pachat-server:
    build: .
    image: pachat-server:latest
    container_name: pachat-server
    restart: unless-stopped
    ports:
      - "9000:9000"
    volumes:
      - pachat-data:/app/data

volumes:
  pachat-data:
